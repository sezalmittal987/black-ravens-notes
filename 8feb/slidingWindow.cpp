#include<iostream>
using namespace std;

int  main(){
    int n, m;
    cin>>n>>m;
    vector<vector<int>> v(n, (vector<int>(m , 0)));
    // input vector
    vector<vector<int>>  vertical(n, (vector<int>(m , 0))), horizontal(n, (vector<int>(m , 0)));
    //vertical 
    //horizontal
}